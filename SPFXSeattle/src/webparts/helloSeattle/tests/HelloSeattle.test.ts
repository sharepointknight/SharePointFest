/// <reference types="mocha" />

import { assert } from 'chai';

describe('HelloSeattleWebPart', () => {
  describe('Test Component', () => {
  it('should do something', () => {
    assert.ok(true);
  });
  it('should do something else', () => {
    assert.equal(true, true);
  });
  });
});
